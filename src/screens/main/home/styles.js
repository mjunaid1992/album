import { StyleSheet } from 'react-native';
import { colors, isIPHONEX, isPAD, wp, hp } from '../../../constants';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: colors.BLACK.background,
        alignItems: 'center',
        justifyContent: 'center'
    },
});

export default styles;
